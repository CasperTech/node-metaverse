// This file has been automatically generated by writePacketClasses.js

import {UUID} from '../UUID';
import {MessageFlags} from '../../enums/MessageFlags';
import {Packet} from '../Packet';

export class RebakeAvatarTexturesPacket implements Packet
{
    name = 'RebakeAvatarTextures';
    flags = MessageFlags.Trusted | MessageFlags.FrequencyLow;
    id = 4294901847;

    TextureData: {
        TextureID: UUID;
    };

    getSize(): number
    {
        return 16;
    }

     writeToBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         this.TextureData['TextureID'].writeToBuffer(buf, pos);
         pos += 16;
         return pos - startPos;
     }

     readFromBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         const newObjTextureData: {
             TextureID: UUID
         } = {
             TextureID: UUID.zero()
         };
         newObjTextureData['TextureID'] = new UUID(buf, pos);
         pos += 16;
         this.TextureData = newObjTextureData;
         return pos - startPos;
     }
}

