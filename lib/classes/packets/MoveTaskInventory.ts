// This file has been automatically generated by writePacketClasses.js

import {UUID} from '../UUID';
import {MessageFlags} from '../../enums/MessageFlags';
import {Packet} from '../Packet';

export class MoveTaskInventoryPacket implements Packet
{
    name = 'MoveTaskInventory';
    flags = MessageFlags.FrequencyLow;
    id = 4294902048;

    AgentData: {
        AgentID: UUID;
        SessionID: UUID;
        FolderID: UUID;
    };
    InventoryData: {
        LocalID: number;
        ItemID: UUID;
    };

    getSize(): number
    {
        return 68;
    }

     writeToBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         this.AgentData['AgentID'].writeToBuffer(buf, pos);
         pos += 16;
         this.AgentData['SessionID'].writeToBuffer(buf, pos);
         pos += 16;
         this.AgentData['FolderID'].writeToBuffer(buf, pos);
         pos += 16;
         buf.writeUInt32LE(this.InventoryData['LocalID'], pos);
         pos += 4;
         this.InventoryData['ItemID'].writeToBuffer(buf, pos);
         pos += 16;
         return pos - startPos;
     }

     readFromBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         const newObjAgentData: {
             AgentID: UUID,
             SessionID: UUID,
             FolderID: UUID
         } = {
             AgentID: UUID.zero(),
             SessionID: UUID.zero(),
             FolderID: UUID.zero()
         };
         newObjAgentData['AgentID'] = new UUID(buf, pos);
         pos += 16;
         newObjAgentData['SessionID'] = new UUID(buf, pos);
         pos += 16;
         newObjAgentData['FolderID'] = new UUID(buf, pos);
         pos += 16;
         this.AgentData = newObjAgentData;
         const newObjInventoryData: {
             LocalID: number,
             ItemID: UUID
         } = {
             LocalID: 0,
             ItemID: UUID.zero()
         };
         newObjInventoryData['LocalID'] = buf.readUInt32LE(pos);
         pos += 4;
         newObjInventoryData['ItemID'] = new UUID(buf, pos);
         pos += 16;
         this.InventoryData = newObjInventoryData;
         return pos - startPos;
     }
}

