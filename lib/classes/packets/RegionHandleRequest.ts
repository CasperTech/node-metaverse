// This file has been automatically generated by writePacketClasses.js

import {UUID} from '../UUID';
import {MessageFlags} from '../../enums/MessageFlags';
import {Packet} from '../Packet';

export class RegionHandleRequestPacket implements Packet
{
    name = 'RegionHandleRequest';
    flags = MessageFlags.FrequencyLow;
    id = 4294902069;

    RequestBlock: {
        RegionID: UUID;
    };

    getSize(): number
    {
        return 16;
    }

     writeToBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         this.RequestBlock['RegionID'].writeToBuffer(buf, pos);
         pos += 16;
         return pos - startPos;
     }

     readFromBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         const newObjRequestBlock: {
             RegionID: UUID
         } = {
             RegionID: UUID.zero()
         };
         newObjRequestBlock['RegionID'] = new UUID(buf, pos);
         pos += 16;
         this.RequestBlock = newObjRequestBlock;
         return pos - startPos;
     }
}

