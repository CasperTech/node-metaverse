// This file has been automatically generated by writePacketClasses.js

import {UUID} from '../UUID';
import {MessageFlags} from '../../enums/MessageFlags';
import {Packet} from '../Packet';

export class GroupProposalBallotPacket implements Packet
{
    name = 'GroupProposalBallot';
    flags = MessageFlags.Deprecated | MessageFlags.FrequencyLow;
    id = 4294902124;

    AgentData: {
        AgentID: UUID;
        SessionID: UUID;
    };
    ProposalData: {
        ProposalID: UUID;
        GroupID: UUID;
        VoteCast: string;
    };

    getSize(): number
    {
        return (this.ProposalData['VoteCast'].length + 1) + 64;
    }

     writeToBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         this.AgentData['AgentID'].writeToBuffer(buf, pos);
         pos += 16;
         this.AgentData['SessionID'].writeToBuffer(buf, pos);
         pos += 16;
         this.ProposalData['ProposalID'].writeToBuffer(buf, pos);
         pos += 16;
         this.ProposalData['GroupID'].writeToBuffer(buf, pos);
         pos += 16;
         buf.write(this.ProposalData['VoteCast'], pos);
         pos += this.ProposalData['VoteCast'].length;
         return pos - startPos;
     }

     readFromBuffer(buf: Buffer, pos: number): number
     {
         const startPos = pos;
         const newObjAgentData: {
             AgentID: UUID,
             SessionID: UUID
         } = {
             AgentID: UUID.zero(),
             SessionID: UUID.zero()
         };
         newObjAgentData['AgentID'] = new UUID(buf, pos);
         pos += 16;
         newObjAgentData['SessionID'] = new UUID(buf, pos);
         pos += 16;
         this.AgentData = newObjAgentData;
         const newObjProposalData: {
             ProposalID: UUID,
             GroupID: UUID,
             VoteCast: string
         } = {
             ProposalID: UUID.zero(),
             GroupID: UUID.zero(),
             VoteCast: ''
         };
         newObjProposalData['ProposalID'] = new UUID(buf, pos);
         pos += 16;
         newObjProposalData['GroupID'] = new UUID(buf, pos);
         pos += 16;
         newObjProposalData['VoteCast'] = buf.toString('utf8', pos, length);
         pos += length;
         this.ProposalData = newObjProposalData;
         return pos - startPos;
     }
}

