// This file has been automatically generated by writeMessageClasses.js

import {MessageFlags} from '../../enums/MessageFlags';
import {MessageBase} from '../MessageBase';
import {Message} from '../../enums/Message';

export class SimulatorMapUpdateMessage implements MessageBase
{
    name = 'SimulatorMapUpdate';
    messageFlags = MessageFlags.Trusted | MessageFlags.FrequencyLow;
    id = Message.SimulatorMapUpdate;

    MapData: {
        Flags: number;
    };

    getSize(): number
    {
        return 4;
    }

    writeToBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        buf.writeUInt32LE(this.MapData['Flags'], pos);
        pos += 4;
        return pos - startPos;
    }

    readFromBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        let varLength = 0;
        const newObjMapData: {
            Flags: number
        } = {
            Flags: 0
        };
        newObjMapData['Flags'] = buf.readUInt32LE(pos);
        pos += 4;
        this.MapData = newObjMapData;
        return pos - startPos;
    }
}

