// This file has been automatically generated by writeMessageClasses.js

import {UUID} from '../UUID';
import {MessageFlags} from '../../enums/MessageFlags';
import {MessageBase} from '../MessageBase';
import {Message} from '../../enums/Message';

export class LiveHelpGroupRequestMessage implements MessageBase
{
    name = 'LiveHelpGroupRequest';
    messageFlags = MessageFlags.Trusted | MessageFlags.FrequencyLow;
    id = Message.LiveHelpGroupRequest;

    RequestData: {
        RequestID: UUID;
        AgentID: UUID;
    };

    getSize(): number
    {
        return 32;
    }

    writeToBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        this.RequestData['RequestID'].writeToBuffer(buf, pos);
        pos += 16;
        this.RequestData['AgentID'].writeToBuffer(buf, pos);
        pos += 16;
        return pos - startPos;
    }

    readFromBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        let varLength = 0;
        const newObjRequestData: {
            RequestID: UUID,
            AgentID: UUID
        } = {
            RequestID: UUID.zero(),
            AgentID: UUID.zero()
        };
        newObjRequestData['RequestID'] = new UUID(buf, pos);
        pos += 16;
        newObjRequestData['AgentID'] = new UUID(buf, pos);
        pos += 16;
        this.RequestData = newObjRequestData;
        return pos - startPos;
    }
}

