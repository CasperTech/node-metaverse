// This file has been automatically generated by writeMessageClasses.js

import {MessageFlags} from '../../enums/MessageFlags';
import {MessageBase} from '../MessageBase';
import {Message} from '../../enums/Message';

export class ReportAutosaveCrashMessage implements MessageBase
{
    name = 'ReportAutosaveCrash';
    messageFlags = MessageFlags.FrequencyLow;
    id = Message.ReportAutosaveCrash;

    AutosaveData: {
        PID: number;
        Status: number;
    };

    getSize(): number
    {
        return 8;
    }

    writeToBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        buf.writeInt32LE(this.AutosaveData['PID'], pos);
        pos += 4;
        buf.writeInt32LE(this.AutosaveData['Status'], pos);
        pos += 4;
        return pos - startPos;
    }

    readFromBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        let varLength = 0;
        const newObjAutosaveData: {
            PID: number,
            Status: number
        } = {
            PID: 0,
            Status: 0
        };
        newObjAutosaveData['PID'] = buf.readInt32LE(pos);
        pos += 4;
        newObjAutosaveData['Status'] = buf.readInt32LE(pos);
        pos += 4;
        this.AutosaveData = newObjAutosaveData;
        return pos - startPos;
    }
}

