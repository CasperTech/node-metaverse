// This file has been automatically generated by writeMessageClasses.js

import { UUID } from '../UUID';
import { MessageFlags } from '../../enums/MessageFlags';
import { MessageBase } from '../MessageBase';
import { Message } from '../../enums/Message';

export class KillChildAgentsMessage implements MessageBase
{
    name = 'KillChildAgents';
    messageFlags = MessageFlags.Trusted | MessageFlags.FrequencyLow;
    id = Message.KillChildAgents;

    IDBlock: {
        AgentID: UUID;
    };

    getSize(): number
    {
        return 16;
    }

    // @ts-ignore
    writeToBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        this.IDBlock['AgentID'].writeToBuffer(buf, pos);
        pos += 16;
        return pos - startPos;
    }

    // @ts-ignore
    readFromBuffer(buf: Buffer, pos: number): number
    {
        const startPos = pos;
        const newObjIDBlock: {
            AgentID: UUID
        } = {
            AgentID: UUID.zero()
        };
        newObjIDBlock['AgentID'] = new UUID(buf, pos);
        pos += 16;
        this.IDBlock = newObjIDBlock;
        return pos - startPos;
    }
}

